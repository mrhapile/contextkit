# Changelog

## 0.1.3

- Add a figlet + gradient-string opening banner for `contextkit init`.
- Add a narrow-terminal fallback banner layout.

## 0.1.2

- Replace the hand-rolled CLI prompts with a branded Clack terminal flow.
- Add per-conflict resolution with diff preview and merge support for `AGENTS.md`.
- Add consistent terminal styling and a richer summary screen.

## 0.1.1

- Add interactive prompts for IDE and install scope.
- Install the full base context pack when requested.
- Expand `AGENTS.md` with the installed skill inventory.

## 0.1.0

- Replace the placeholder CLI with a functional `init` command.
- Add manifest-based, rerunnable installs.
- Add shared workflow templates and agent-specific entrypoints.
- Add automated tests for install and conflict behavior.
