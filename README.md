# contextkit

`contextkit` is an upcoming CLI for installing AI-agent workflow context files and skills into a project.

This package is a placeholder while the real installer is being built.

Coming soon: `npx @akashanand/contextkit init` and support for multiple agent targets, including Claude, Codex, Antigravity, and Cursor.
